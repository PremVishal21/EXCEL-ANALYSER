# Backend

Run `npm install` and `npm start` after setting .env MONGO_URI and JWT_SECRET.
Endpoints:
- POST /api/auth/register
- POST /api/auth/login
- POST /api/upload/excel (multipart form-data file)